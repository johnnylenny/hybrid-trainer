# macOS
.DS_Store
.AppleDouble
.LSOverride

# Windows
Thumbs.db
ehthumbs.db
Desktop.ini

# Editors
.vscode/
.idea/
*.swp
*.swo
*~

# Logs
*.log

# Python bytecode cache (sync/ scripts)
__pycache__/
*.pyc

# Personal backups (don't commit your workout data to a public repo)
*.backup.json
hybrid-trainer-backup-*.json

# Claude Code dev tooling + local settings (not part of the live app)
.claude/

# Private working docs kept local, never pushed to the public repo
CLAUDE.md
IDEAS.md
_local/
outputs/
_progress.md

# Garmin sync local debug log (dates + activity types; keep out of the public repo)
sync_log.txt

# Garmin outbound push: saved workout JSON reference payloads (personal
# workout content, not app code; keep out of the public repo)
sync/testdata/
